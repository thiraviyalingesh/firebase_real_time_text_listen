import 'dart:io';

class AppFontSize {
  AppFontSize._();

  static double fontSize10 = Platform.isIOS ? 12.0 : 10.0;
  static double fontSize11 = Platform.isIOS ? 13.0 : 11.0;
  static double fontSize12 = Platform.isIOS ? 14.0 : 12.0;
  static double fontSize13 = Platform.isIOS ? 15.0 : 13.0;
  static double fontSize14 = Platform.isIOS ? 16.0 : 14.0;
  static double fontSize15 = Platform.isIOS ? 17.0 : 15.0;
  static double fontSize16 = Platform.isIOS ? 18.0 : 16.0;
  static double fontSize17 = Platform.isIOS ? 19.0 : 17.0;
  static double fontSize18 = Platform.isIOS ? 20.0 : 18.0;
  static double fontSize19 = Platform.isIOS ? 21.0 : 19.0;
  static double fontSize20 = Platform.isIOS ? 22.0 : 20.0;
  static double fontSize21 = Platform.isIOS ? 23.0 : 21.0;
  static double fontSize22 = Platform.isIOS ? 24.0 : 22.0;
  static double fontSize24 = Platform.isIOS ? 26.0 : 24.0;
  static double fontSize25 = Platform.isIOS ? 27.0 : 25.0;
  static double fontSize26 = Platform.isIOS ? 28.0 : 26.0;
  static double fontSize27 = Platform.isIOS ? 29.0 : 27.0;
  static double fontSize30 = Platform.isIOS ? 32.0 : 30.0;
  static double fontSize50 = Platform.isIOS ? 52.0 : 50.0;
}
